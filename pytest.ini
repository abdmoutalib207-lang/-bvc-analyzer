[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers --tb=short
# Les tests ne touchent ni le réseau ni les fichiers de production ; ils sont
# rapides par construction (moins d'une seconde pour la suite entière).
