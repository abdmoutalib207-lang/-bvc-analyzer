# Dépendances de DÉVELOPPEMENT — jamais installées par les runs de production.
#
# Séparées de requirements.txt à dessein : les workflows de collecte tournent
# quatre fois par jour ouvré et n'ont aucun besoin de pytest. Les y ajouter
# allongerait chaque run sans rien apporter.
#
# Installation : pip install -r requirements_dev.txt
-r requirements.txt
pytest>=8.0

# Lecture des workflows par tests/test_workflow_tests.py. ⚠️ Sans cette ligne,
# le test s'abstenait en CI — et c'est en CI qu'il doit parler : il vérifie que
# le workflow juge bien le fichier qu'il prétend juger.
PyYAML>=6.0
